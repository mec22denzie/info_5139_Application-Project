# info_5139_Application-Project

## Firebase Setup
1. Create `.env` from `.env.example`.
2. Paste the shared Firebase config values into `.env`.
3. In Firebase Console, enable Email/Password Auth and Firestore.
4. Run `npm install` then `npm start`.
